# Development & Testing Dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
# Required by FastAPI at import time for UploadFile routes (/search/image);
# without it, every API test errors during app initialization.
python-multipart>=0.0.9
